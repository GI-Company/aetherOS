rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated users can access storage
    match /users/{userId}/{allPaths=**} {
      // A user can read or write to their own folder
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
